# PyTorch (install separately: https://pytorch.org)
# torch>=2.1.0
# torchvision>=0.16.0

# Core
accelerate>=0.27.0
diffusers>=0.27.0
transformers>=4.38.0
peft>=0.9.0
open_clip_torch>=2.24.0
ruamel.yaml

# Vision / evaluation
Pillow
scipy
numpy
pandas
rapidfuzz
torchmetrics[image]   # for FID

# Emergent language model
# (DINOv2 is loaded via torch.hub from facebookresearch/dinov2)
